/* scope of variables - region from where a variable can be accessed.
 *               1 class-level scope
 *               2  method-level scope
 *               3 block-level scope
 * Formal parameters or parameters - defined during function definition.
 *                     e.g.- sum(int a, int b) {} (int a, int b)-a.p.
 * Actual parameters - e.g. - sum(x , y)
 * Pass by value - 1. copies the value of actual parameter
 *                 2. called function creates its own copies
 * Pass by Reference - 1. passes the parameter as reference(address)
 *                     2. called function does not create its own copy
 */

public class methodsandscope {
    public static void main(String[] args) {
                
    }
}
